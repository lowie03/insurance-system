# Data you may not redistribute (Zindi competition rules)
data/raw/

# Generated artifacts: rebuilt by the training scripts
artifacts/models/
*.joblib

# Secrets: never committed. Covers .env, .env.local, .env.production, etc. everywhere in the repo
# (both backend/ and frontend/) -- but .env.example files are templates with no real values, so
# those stay tracked.
.env*
!.env.example
*signing_key*

# Runtime data
*.db
policies/
backend/storage/

# Python
__pycache__/
*.pyc
.venv/
*.egg-info/
.pytest_cache/

# Node
node_modules/
dist/

# OS / editor
.DS_Store
.vscode/*
!.vscode/settings.json